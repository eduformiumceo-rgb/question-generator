name = "eduformium-questions-generator"
compatibility_date = "2024-11-01"
pages_build_output_dir = "dist"

# KV namespace used for OTP rate-limiting (functions/api/auth/[[path]].js).
# Bind this to the same namespace the Lesson Planner uses, or create a new
# one — see README_INTEGRATION.md §3.
# [[kv_namespaces]]
# binding = "OTP_KV"
# id = "REPLACE_WITH_YOUR_KV_NAMESPACE_ID"
